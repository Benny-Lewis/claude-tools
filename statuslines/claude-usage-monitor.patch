diff --git a/statusline.py b/statusline.py
index 82ac094..f57c587 100644
--- a/statusline.py
+++ b/statusline.py
@@ -32,7 +32,7 @@ SHOW_RESET = os.environ.get("CQB_RESET", "1") == "1"
 SHOW_DURATION = os.environ.get("CQB_DURATION", "1") == "1"
 SHOW_BRANCH = os.environ.get("CQB_BRANCH", "1") == "1"
 SHOW_COST = os.environ.get("CQB_COST", "0") == "1"
-SHOW_REMAINING = os.environ.get("CQB_REMAINING", "1") == "1"
+SHOW_REMAINING = os.environ.get("CQB_REMAINING", "0") == "1"
 SHOW_BAR = os.environ.get("CQB_BAR", "1") == "1"
 
 # ── Read stdin ──────────────────────────────────────────────────
@@ -153,31 +153,54 @@ def format_duration(ms):
     return f"{ms // 1000}s"
 
 
-def format_reset(minutes):
-    """Format reset countdown."""
+def format_reset(minutes, used_pct=None, window_min=None):
+    """Format reset countdown with pacing-aware color."""
     if minutes is None:
         return ""
     m = int(minutes)
+    # Determine color based on pacing
+    tc = D  # default: dim gray
+    if used_pct is not None and window_min:
+        elapsed_pct = (window_min - m) / window_min * 100
+        gap = int(used_pct) - elapsed_pct
+        if gap >= 20:
+            tc = R  # running hot — slow down
+        elif gap <= -20:
+            tc = C  # way under pace — room to use more
     if m >= 1440:
-        return f" {D}({m // 1440}d){N}"
+        return f" {tc}{m // 1440}d{(m % 1440) // 60}h{N}"
     if m >= 60:
-        return f" {D}({m // 60}h){N}"
-    return f" {D}({m}m){N}"
+        return f" {tc}{m // 60}h{m % 60}m{N}"
+    return f" {tc}{m}m{N}"
 
 
 
-def used_pct_str(used_pct):
-    """Format used or remaining % with color."""
+def used_pct_str(used_pct, remain_min=None, window_min=None):
+    """Format used or remaining % with color and optional pace marker."""
     if used_pct is None or used_pct == "--":
         return "--"
     used = int(used_pct)
     c = color_pct(used)
     val = 100 - used if SHOW_REMAINING else used
     if SHOW_BAR:
-        filled = round(min(100, max(0, val)) / 100.0 * 5)
-        filled_chars = "\u25b0" * filled
-        empty_chars = "\u25b1" * (5 - filled)
-        bar = f"{c}{filled_chars}{empty_chars}{N} "
+        width = 10
+        filled = round(min(100, max(0, val)) / 100.0 * width)
+        # Pace marker: where even usage would put you
+        pace_pos = -1
+        if remain_min is not None and window_min:
+            elapsed_pct = (window_min - remain_min) / window_min * 100
+            pace_pos = round(min(100, max(0, elapsed_pct)) / 100.0 * width)
+            pace_pos = min(pace_pos, width - 1)
+        # Build bar character by character (■□ with white • pace marker)
+        chars = []
+        for i in range(width):
+            if i == pace_pos:
+                chars.append(f"{N}\u25a0{c}")  # white ■ (same shape, different color)
+            elif i < filled:
+                chars.append("\u25a0")  # black square ■
+            else:
+                chars.append("\u25a1")  # white square □
+        bar = f"{c}{''.join(chars)}{N} "
     else:
         bar = ""
     return f"{bar}{c}{val}%{N}"
@@ -361,10 +384,10 @@ def read_cached_usage():
 SEP = " \u2502 "  # │
 DIAMOND = "\u25c6"  # ◆
 
-# Context gauge (5 blocks)
-ctx_remaining = 100 - ctx_pct_used
-filled = round(min(100, max(0, ctx_remaining)) / 100.0 * 5)
-gauge = "\u25b0" * filled + "\u25b1" * (5 - filled)  # ▰▱
+# Context gauge (10 blocks)
+ctx_display = ctx_pct_used if not SHOW_REMAINING else 100 - ctx_pct_used
+filled = round(min(100, max(0, ctx_display)) / 100.0 * 10)
+gauge = "\u25a0" * filled + "\u25a1" * (10 - filled)  # ■□
 
 # Context size label
 if ctx_size >= 1_000_000:
@@ -381,18 +404,22 @@ if proj_name:
         loc = loc[:39] + "\u2026"
     line1_parts.append(loc)
 
+if SHOW_DURATION:
+    line1_parts.append(f"{D}{format_duration(duration_ms)}{N}")
+
 line1 = SEP.join(line1_parts)
 
-# Line 2: context gauge, quota, duration
+# Line 2: context gauge, quota
 ctx_color = color_pct(ctx_pct_used)
-ctx_str = f"{ctx_color}{gauge}{N} {ctx_remaining}%"
+ctx_tokens_used = in_tok + out_tok
+ctx_str = f"{ctx_color}{gauge}{N} {compact(ctx_tokens_used)}"
 if SHOW_CONTEXT_SIZE:
     ctx_str += f" of {ctx_label}"
 line2_parts = [ctx_str]
 
-# Token counts
-if SHOW_TOKENS and (in_tok or out_tok):
-    line2_parts.append(f"\u2191{compact(in_tok)} \u2193{compact(out_tok)}")
+# Token counts (disabled — context bar is sufficient)
+# if SHOW_TOKENS and (in_tok or out_tok):
+#     line2_parts.append(f"\u2191{compact(in_tok)} \u2193{compact(out_tok)}")
 
 # Quota
 usage = read_cached_usage()
@@ -404,11 +431,11 @@ if usage:
 
     pace5 = pace_indicator(u5, r5, 300) if SHOW_PACE else ""
     pace7 = pace_indicator(u7, r7, 10080) if SHOW_PACE else ""
-    reset5 = format_reset(r5) if SHOW_RESET else ""
-    reset7 = format_reset(r7) if (SHOW_RESET and u7 is not None and int(u7) >= 70) else ""
+    reset5 = format_reset(r5, u5, 300) if SHOW_RESET else ""
+    reset7 = format_reset(r7, u7, 10080) if SHOW_RESET else ""
 
-    line2_parts.append(f"5h: {used_pct_str(u5)}{pace5}{reset5}")
-    line2_parts.append(f"7d: {used_pct_str(u7)}{pace7}{reset7}")
+    line2_parts.append(f"5h: {used_pct_str(u5, r5, 300)}{pace5}{reset5}")
+    line2_parts.append(f"7d: {used_pct_str(u7, r7, 10080)}{pace7}{reset7}")
 
     # Extra usage (only show when 5h is nearly exhausted)
     if usage["extra_enabled"] and u5 is not None and int(u5) >= 80:
@@ -427,10 +454,6 @@ else:
 if SHOW_COST and cost_usd > 0:
     line2_parts.append(f"{D}${cost_usd:.2f}{N}")
 
-# Duration
-if SHOW_DURATION:
-    line2_parts.append(f"{D}{format_duration(duration_ms)}{N}")
-
 line2 = SEP.join(line2_parts)
 
 print(line1)
